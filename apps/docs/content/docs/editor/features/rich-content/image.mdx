---
title: Image
description: Embed images with `setImage`.
---



Type: **Node**

Image embeds an image into the document. Renders as `<img src="..." alt="...">` in HTML output.

## Commands

```ts
editor.runCommand("setImage", {
  src: "https://example.com/photo.jpg",
  alt: "Description of image",
});
```

## Active State

```ts
const isActive = editor.isActive("image");
```

## Demo

<FeatureDemo
  feature="image"
  content="<p>Try the image feature here</p>"
  commands={[{ label: "\U0001f5bc", command: "setImage", isActive: "image" }]}
  code='editor.runCommand("setImage", { src: "https://example.com/photo.jpg" })'
/>
