---
title: Strike
description: Strikethrough text with `toggleStrike`.
---



Type: **Mark**

Strike draws a line through text to indicate deletion. Renders as `<s>` in HTML output.

## Commands

```ts
editor.runCommand("toggleStrike");
```

## Keyboard Shortcut

**Mod + Shift + S**

## Active State

```ts
const isActive = editor.isActive("strike");
```

## Demo

<FeatureDemo
  feature="strike"
  content="<p>Sometimes you need to <s>strike through</s> text to show corrections or outdated information.</p>"
  commands={[{ label: "Strike", command: "toggleStrike", isActive: "strike", icon: "Strike" }]}
/>
