---
title: Code
description: Format inline code with `toggleCode`.
---



Type: **Mark**

Code renders inline text in a monospace font for code snippets or technical terms. Renders as `<code>` in HTML output.

## Commands

```ts
editor.runCommand("toggleCode");
```

## Keyboard Shortcut

**Mod + E**

## Active State

```ts
const isActive = editor.isActive("code");
```

## Demo

<FeatureDemo
  feature="code"
  content="<p>Use <code>inline code</code> for snippets, variable names, or technical terms within a sentence.</p>"
  commands={[{ label: "Code", command: "toggleCode", isActive: "code", icon: "Code" }]}
/>
